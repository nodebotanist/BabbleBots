#include "application.h"
<% includes.forEach(function(incl){ %>
<%- incl %>
<% }); %>

SYSTEM_MODE(AUTOMATIC);

<% preInit.forEach(function(pre){ %>
<%- pre %>
<% }) %>
void setup(){
<% init.forEach(function(i){ %>
  <%- i %>
<% }) %>
}

void loop(){
<% loop.forEach(function(l){ %>
  <%- l %>
<% }) %>
}

<% customFunctions.forEach(function(custom){ %>
<%- custom %>
<% }) %>